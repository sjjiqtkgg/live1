name: Keep Render Service Alive

on:
  schedule:
    # 每14分钟执行一次（全天24小时）
    - cron: '*/14 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render health endpoint
        run: curl -X GET https://live1-cxe9.onrender.com/health
